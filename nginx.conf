server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        try_files $uri /index.html;  # 모든 요청을 index.html로 리다이렉트
    }

    error_page 404 /index.html;  # 404 에러 시 index.html 제공
}
